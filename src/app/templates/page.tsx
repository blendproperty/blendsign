import FeaturePage from "@/components/FeaturePage";

export default function Templates() {
  return <FeaturePage eyebrow="Reusable workflows" title="Templates" description="Build approved property documents once, then prepare them for signature in a few clicks." icon="template" action="Create a template" bullets={["Standardise frequently used agreements", "Reduce repeated document preparation", "Keep approved wording consistent"]} />;
}
