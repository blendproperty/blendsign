import FeaturePage from "@/components/FeaturePage";

export default function SignForms() {
  return <FeaturePage eyebrow="Public signing links" title="SignForms" description="Collect completed and signed documents through a secure link that can be shared with tenants, suppliers or applicants." icon="link" action="Create a SignForm" bullets={["Share one secure signing link", "Collect structured recipient details", "Track every submission and audit event"]} />;
}
